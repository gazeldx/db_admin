source 'http://rubygems.org'

gem 'sequel'
gem 'sinatra'
gem 'sqlite3'

# You need to add your database driver gem here like:
# gem 'pg' #Postgresql database driver gem. And also you can do the same work by `$ gem install pg`